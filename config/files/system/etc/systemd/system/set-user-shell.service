[Unit]
Description=Set user shell to nushell
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/usr/bin/bash -c 'for user in $(ls /home); do usermod -s /usr/bin/nu $user; done'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
